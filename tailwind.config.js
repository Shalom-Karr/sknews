/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./*.{html,js}" // Tells Tailwind to scan all HTML and JS files in the root folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
